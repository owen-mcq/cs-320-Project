/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "cdn-exercisedb.vercel.app",
      "cdn3.iconfinder.com",
      "cdn2.iconfinder.com",
    ],
  },
};

export default nextConfig;
